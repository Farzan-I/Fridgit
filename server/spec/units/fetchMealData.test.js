import { getMealData } from "../../controllers/mealData";
// need to mock the above

describe("getMealData", () => {
  it("Sends meal data to Client", () => {
    // getMealData()
    expect(1).toEqual(1)
  })
})